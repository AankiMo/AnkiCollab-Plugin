[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
norecursedirs = .git .venv __pycache__ crowd_anki ui dist MagicMock notifications_webview
asyncio_mode = auto
markers =
    integration: workflow-level tests (tests/integration/) that exercise the real
        export/import/media orchestration against a realistic in-memory collection
addopts = -v --tb=short --import-mode=importlib
